# local data stores — never commit market data
data/
*.duckdb
*.duckdb.wal

# PIT evidence packets: licensed data + case identities stay local
packets/

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# secrets — API keys live in env vars or ~/.nasdaq, never the repo
.env
.env.*
